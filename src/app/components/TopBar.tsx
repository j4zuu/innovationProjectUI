import Hamburger from "@/app/components/Hamburger";

const TopBar = () => {
    return (
        <div className="topBarStyle">
            <Hamburger>
            </Hamburger>
        </div>

    );
}

export default TopBar